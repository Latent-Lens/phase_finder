{
  "name": "phasefinder",
  "version": "0.8.0",
  "description": "PhaseFinder is a browser-based tool for inspecting and plotting flow cytometry `.fcs` files. It is designed as a lightweight, local-first workspace: users drop FCS files into the page, the app reads the FCS header and TEXT metadata in the browser, and selected samples can then be loaded into an overlaid DNA-content event histogram with optional Dean–Jett–Fox cell-cycle modeling.",
  "private": true,
  "packageManager": "npm@11.13.0",
  "engines": {
    "node": "24.x"
  },
  "directories": {
    "doc": "docs",
    "test": "tests"
  },
  "scripts": {
    "preflight": "node scripts/preflight.cjs",
    "dev": "vite",
    "build": "vite build && npm run provenance",
    "preview": "vite preview",
    "test:ci": "python3 -m unittest discover -s tests/ci",
    "test:unit": "python3 tests/unit/driving_code/run_standalone.py --limited-media",
    "test:e2e": "python3 tests/e2e/driving_code/drive_flow.py",
    "test:audit": "python3 tests/e2e/driving_code/drive_flow.py --skip-modeling --limited-media && npm run build && npm run test:dist",
    "test:dist": "python3 tests/e2e/driving_code/dist_smoke.py",
    "test": "npm run test:ci && npm run test:unit",
    "check:dist": "node scripts/verify-dist.cjs",
    "check:docs": "python3 scripts/check_documents.py",
    "check:dom": "node scripts/check-dom-bindings.mjs",
    "check:imports": "python3 scripts/check_import_graph.py",
    "check:privacy": "git ls-files -z | node scripts/check-privacy.cjs",
    "lint:js": "eslint js scripts vite.config.js",
    "check:base": "BASE_PATH=/phasefinder/ vite build --outDir dist-base --emptyOutDir && DIST_DIR=dist-base npm run provenance && BASE_PATH=/phasefinder/ DIST_DIR=dist-base npm run check:dist",
    "report:size": "node scripts/report-artifact-delta.cjs",
    "provenance": "node scripts/generate-provenance.cjs",
    "check": "npm run preflight && npm run lint:js && npm run check:dom && npm run check:docs && npm run check:imports && npm run check:privacy && npm test && npm run build && npm run check:dist && npm run check:privacy"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Latent-Lens/PhaseFinder.git"
  },
  "keywords": [],
  "author": "",
  "license": "PolyForm-Noncommercial-1.0.0",
  "type": "commonjs",
  "bugs": {
    "url": "https://github.com/Latent-Lens/PhaseFinder/issues"
  },
  "homepage": "https://github.com/Latent-Lens/PhaseFinder#readme",
  "devDependencies": {
    "axe-core": "^4.12.1",
    "eslint": "^10.8.0",
    "globals": "^17.8.0",
    "vite": "^8.1.5"
  }
}
